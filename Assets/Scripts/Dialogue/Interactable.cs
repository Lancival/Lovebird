using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Interactable : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame The Update function 
    //is the place to put code that will handle the frame 
    //update for the GameObject. This might include movement, 
    //triggering actions and responding to user input, basically 
    //anything that needs to be handled over time during gameplay.
    void Update()
    {
        
    }
}
